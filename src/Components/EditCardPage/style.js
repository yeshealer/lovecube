import { wrapn } from "wrapn";

export const Body = wrapn('div')`
    w-full flex flex-col items-center justify-center
`

export const SubTitle = wrapn('div')`
    text-lg xl:text-[24px] py-4 text-center
`